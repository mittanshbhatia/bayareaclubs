# Migrations

Add immutable, timestamp-prefixed Supabase migrations here. Every exposed table
must enable RLS and define explicit operation policies in the same feature change.
Regenerate TypeScript database types after each schema change.
